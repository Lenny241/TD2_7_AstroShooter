<Window x:Class="AstroShooter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="AstroShooter" 
        ResizeMode="NoResize"
        KeyDown="Window_KeyDown" 
        KeyUp="Window_KeyUp"
        Focusable="True"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen" WindowStyle="None" Icon="/asset/character/vaisseau.png">

    <Grid x:Name="gridMain" Height="850" Width="1375">

        <!-- Conteneur pour le menu, règles, paramètres, etc. -->
        <Grid x:Name="ScreenContainer" Panel.ZIndex="10" >
        </Grid>

        <!-- Canvas du jeu -->
        <Canvas x:Name="GameCanvas" ClipToBounds="True" Opacity="0.9" Focusable="True" MouseLeftButtonDown="GameCanvas_MouseLeftButtonDown">
            <Canvas.Effect>
                <BlurEffect RenderingBias="Quality" Radius="10"/>
            </Canvas.Effect>
            <TextBlock Panel.ZIndex="100" x:Name="InterstellarNuggetsCount" HorizontalAlignment="Left" Margin="42,786,0,0" TextWrapping="Wrap" Text="Interstellar Nuggets : 0" VerticalAlignment="Top" FontFamily="Trebuchet MS" FontSize="20">
                <TextBlock.Foreground>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="Black"/>
                        <GradientStop Color="#FF340EEE" Offset="1"/>
                    </LinearGradientBrush>
                </TextBlock.Foreground>
            </TextBlock>
        </Canvas>
    </Grid>

</Window>
